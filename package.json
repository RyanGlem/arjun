{
  "name": "raycasting",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "ts-node-dev --respawn  src/server.ts",
    "build": "esbuild src/world.ts --bundle --outfile=src/bundle.js",
    "watch": "esbuild src/world.ts --outfile=src/bundle.js --bundle --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "colyseus": "^0.15.0",
    "esbuild": "^0.17.11"
  }
}
